name: LWU
mnemonic: lwu
format: I
fixed_values:
  opcode: 0x03
  funct3: 0x6
inputs:
  - rd
  - rs1
  - imm
extension: "I"
description: |
  LWU loads a word from memory, zero-extends it, and stores it in rd.
  rd = zero_extend(MEM[rs1 + imm][31:0])
