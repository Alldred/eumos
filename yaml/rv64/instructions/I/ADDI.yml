name: ADDI
format: I.yml
fixed_values:
  opcode: 0x13
  funct3: 0x0
inputs:
  - rd
  - rs1
  - imm
description: |
  ADDI adds the value in rs1 and the immediate, and stores the result in rd.
  rd = rs1 + imm